## 1. Pure math and the feed loader (≈60 min)

- [ ] 1.1 Add `ideaProgress(signal, now?)`, `progressSentence(signal, now?)` and `groupCopyMarkers(copies)` to `lib/signals/math.ts` per design D1, D2 and D6, with tests in `lib/signals/math.test.ts`: Up and Down positions (entry at `slPct / (tpPct + slPct)`, "now" a quarter of the way to TP for the spec example), clamping past TP and past SL, the ±0.2 % "at-entry" band, `null` price → `"unknown"` and "live price unavailable", expired idea → "this idea has ended", and marker grouping for one, two and three copies in one minute plus copies in different minutes. Verify: `pnpm test` green.
- [ ] 1.2 Add `getPrices(marketNames)` to `lib/decibel/markets.ts` (one `markets.getAll()`, then `Promise.allSettled` over the mids; unknown or failing markets omitted) and `lib/signals/feed.ts` with `loadFeed()` returning `SignalList` plus `prices` (distinct markets of live signals only); extend `SignalList` in `lib/schemas.ts`; export through `lib/signals/index.ts`; make `app/page.tsx` and `GET /api/signals` call it. Verify: `pnpm typecheck` and `pnpm lint` exit 0; with the dev server, `curl localhost:3000/api/signals` shows `prices` with a positive BTC/USD mid and no key for an expired idea's market; the feed page still renders the cards.

## 2. The card and the chart markers (≈75 min)

- [ ] 2.1 Create `components/IdeaStrip.tsx` (SVG per design D4: track, three ticks with labels and prices, down-colour and up-colour segments, yellow "now" dot, `role="img"` with the sentence as `aria-label`) and rebuild `components/SignalCard.tsx` per D5: identity line, headline "BTC goes up ↑" / "goes down ↓" in the direction colour, strip, sentence, pills and status, button "See it on the chart" (expired: "See how it went"). Verify with agent-browser at 375 px against the dev server: a live Up card shows the strip with Stop loss left, Entry between, Take profit right, the "now" dot and the sentence; a Down card keeps stop left / take profit right; exactly one yellow action kind on the screen (computed background colour count); no jargon word from the constitution list in the visible text; one full card plus the next headline fit at 375 × 812.
- [ ] 2.2 Group same-minute copies in `components/SignalDetail.tsx` with `groupCopyMarkers` before passing markers to `PriceChart`. Verify: open Ana's deployed idea (two copies in one minute) locally against Turso — one marker "Ben, Ben copied" (or the grouped count) and no overlapping labels in a 375 px screenshot.
- [ ] 2.3 Price-unavailable path: block `/api/price` upstream by pointing `getPrices` at a market that does not exist in a temporary edit (or stub in a test) and reload the feed. Verify: the strip draws the three levels without a "now" dot and the sentence reads "live price unavailable"; restore afterwards; `pnpm test`, `pnpm lint`, `pnpm build` green.

## 3. Documentation and the live check (≈30 min)

- [ ] 3.1 README: status row SHOULD 6 says "feed strip with the live price + detail chart"; demo path step 4 names "See it on the chart"; "Try the deployed demo" sentence updated; replace `docs/signal-chart.png` caption if the feed screenshot is added next to it. Interview guide (outside the repo) gains the section for this change. Verify: `grep -n "Copy\b" README.md` shows no card-button reference to the old label.
- [ ] 3.2 After the push and the Vercel deploy, open the live feed at 375 px with cleared storage. Verify: the cards draw the strips with a "now" dot and sentence without any code; tapping "See it on the chart" opens the chart; the two same-minute copies on Ana's idea render as one marker. Record the deployment URL in the commit body.

## 4. Change review

- [ ] 4.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with one extra question: does any card render depend on a fetch that can fail (it must not — the strip must draw from the signal alone). Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded in this file.
