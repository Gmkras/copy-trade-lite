## 1. Pure math and the feed loader (≈60 min)

- [x] 1.1 Add `ideaProgress(signal, now?)`, `progressSentence(signal, now?)` and `groupCopyMarkers(copies)` to `lib/signals/math.ts` per design D1, D2 and D6, with tests in `lib/signals/math.test.ts`: Up and Down positions (entry at `slPct / (tpPct + slPct)`, "now" a quarter of the way to TP for the spec example), clamping past TP and past SL, the ±0.2 % "at-entry" band, `null` price → `"unknown"` and "live price unavailable", expired idea → "this idea has ended", and marker grouping for one, two and three copies in one minute plus copies in different minutes. Verify: `pnpm test` green.
- [x] 1.2 Add `getPrices(marketNames)` to `lib/decibel/markets.ts` (one `markets.getAll()`, then `Promise.allSettled` over the mids; unknown or failing markets omitted) and `lib/signals/feed.ts` with `loadFeed()` returning `SignalList` plus `prices` (distinct markets of live signals only); extend `SignalList` in `lib/schemas.ts`; export through `lib/signals/index.ts`; make `app/page.tsx` and `GET /api/signals` call it. Verify: `pnpm typecheck` and `pnpm lint` exit 0; with the dev server, `curl localhost:3000/api/signals` shows `prices` with a positive BTC/USD mid and no key for an expired idea's market; the feed page still renders the cards.

## 2. The card and the chart markers (≈75 min)

- [x] 2.1 Create `components/IdeaStrip.tsx` (SVG per design D4: track, three ticks with labels and prices, down-colour and up-colour segments, yellow "now" dot, `role="img"` with the sentence as `aria-label`) and rebuild `components/SignalCard.tsx` per D5: identity line, headline "BTC goes up ↑" / "goes down ↓" in the direction colour, strip, sentence, pills and status, button "See it on the chart" (expired: "See how it went"). Verify with agent-browser at 375 px against the dev server: a live Up card shows the strip with Stop loss left, Entry between, Take profit right, the "now" dot and the sentence; a Down card keeps stop left / take profit right; exactly one yellow action kind on the screen (computed background colour count); no jargon word from the constitution list in the visible text; one full card plus the next headline fit at 375 × 812.
- [x] 2.2 Cluster copies close in time in `components/SignalDetail.tsx` with `groupCopyMarkers` (ten-minute window, design D6) before passing markers to `PriceChart`. Verify: open Ana's deployed idea (two copies nine minutes apart) locally against Turso — one marker "Ben, You copied" and no overlapping labels in a 375 px screenshot.
- [x] 2.3 Fallback paths with temporary edits, restored afterwards: (a) make `getCandlesFor` return `{}` and reload the feed — every card draws the strip with the "now" dot and the sentence; (b) also make `getPrices` return `{}` — the strip has no "now" dot and the sentence reads "live price unavailable". Verify each with a 375 px screenshot; then `pnpm test`, `pnpm lint`, `pnpm build` green.
- [x] 2.4 (added after the user asked for a TradingView chart on every card, all the time) Add `getCandlesFor` to `lib/decibel/markets.ts` and `candles` to `loadFeed()`/`SignalList`; `SignalCard` renders `PriceChart` (design D4: 170 px, the three price lines, no markers, stable empty marker list so polls do not rebuild unchanged charts) and falls back to `IdeaStrip` without candles. Verify with agent-browser at 375 px: the BTC card draws BTC candles and the AMZN card AMZN candles, each with its three labelled lines and the last price on the axis; `curl /api/signals` shows ~60 candles per live market; one full card plus the next headline still fit at 375 × 812.

## 3. Documentation and the live check (≈30 min)

- [x] 3.1 README: status row SHOULD 6 says "feed strip with the live price + detail chart"; demo path step 4 names "See it on the chart"; "Try the deployed demo" sentence updated; replace `docs/signal-chart.png` caption if the feed screenshot is added next to it. Interview guide (outside the repo) gains the section for this change. Verify: `grep -n "Copy\b" README.md` shows no card-button reference to the old label.
- [x] 3.2 After the push and the Vercel deploy, open the live feed at 375 px with cleared storage. Verify: the cards draw the strips with a "now" dot and sentence without any code; tapping "See it on the chart" opens the chart; the two same-minute copies on Ana's idea render as one marker. Record the deployment URL in the commit body.

## 4. Change review

- [x] 4.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with one extra question: does any card render depend on a fetch that can fail (it must not — the strip must draw from the signal alone). Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded in this file.

  **P-R result (2026-09-07): no findings.** `SignalCard` and `IdeaStrip` contain no `fetch`, `usePoll` or `await`: a card renders from the signal plus whatever the feed already carried, and both fallbacks were exercised on screen (2.3). No `any`, `@ts-ignore`, empty `catch`, `NEXT_PUBLIC_` or `MAINNET` in `app/`, `components/`, `hooks/`, `lib/`, `scripts/`. No new write path: the change only adds reads (`getPrices`, `getCandlesFor`) behind `Promise.allSettled`, each failure logged and omitted. Typecheck, lint, 108 tests and the production build are green. The two design reversals (strip → SVG chart → real chart) happened inside the change at the user's request and are kept as `design.md.old` / `.old2`.
