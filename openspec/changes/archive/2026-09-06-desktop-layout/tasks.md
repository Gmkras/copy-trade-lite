## 1. Foundation: hook, nav, background (≈60 min)

- [x] 1.1 Create `hooks/useMediaQuery.ts` (`useSyncExternalStore`, server snapshot `false`, subscribes to `matchMedia(query)` changes) and export `WIDE = "(min-width: 1024px)"`. Verify: `pnpm typecheck` and `pnpm lint` exit 0; a throwaway `console.log` in the Trade page shows `false` on the server render and `true` after hydration at 1280 px (remove it).
- [x] 1.2 Rename `BottomNav` to `AppNav` per design D3: inline-SVG icon + label per tab, `aria-current`, opaque `bg-bg` (no blur), `padding-bottom: env(safe-area-inset-bottom)` below 1024 px; top bar with the wordmark and the "play money · testnet" chip from 1024 px; `body` paddings `pb-16 lg:pb-0 lg:pt-16`; body background gradient (D5) in `globals.css`; constitution P4 check reworded. Verify with agent-browser: at 375 px the bar is at the bottom, opaque (computed `backdrop-filter: none`, background alpha 1), both tabs ≥ 44 px tall with an icon and a label, active tab yellow; at 1280 px the bar is at the top with the wordmark; screenshots of both.

## 2. Two columns (≈90 min)

- [x] 2.1 Feed side by side (D2, D4): `app/page.tsx` renders `Feed` in the left column and a new `components/DetailRail.tsx` (client; `useMediaQuery(WIDE)`; renders `SignalDetail` for the newest live idea from the feed data, or an inviting empty panel) in the right; `app/signals/[id]/page.tsx` renders a new `components/FeedRail.tsx` (client; polls `/api/signals` only when wide, renders the cards) on the left and the detail on the right; both pages `lg:grid lg:max-w-6xl`. Verify with agent-browser: at 1280 px `/` shows the list and the newest idea's chart + copy panel; "See it on the chart" on another card changes the URL and the right column; at 375 px `/signals/[id]` makes no request to `/api/signals` (network log) and looks as before.
- [x] 2.2 Trade as a trading desk (design D4, revised after measuring): `TradeScreen` owns the selected coin and the price poll and lays out `CoinPills` / `TradeForm` (ticket) / new `MarketPanel` (hero + chart) / `AccountCard`; phone order preserved with `order` classes. Verify: at 1280 × 800 the yellow button and the account's numbers are both visible without scrolling and the chart is ≥ 360 px; at 1024 the account runs full width beneath; at 375 × 812 the button's bottom edge is still above the nav (re-measure, target ≤ 755).
- [x] 2.3 Sheet as a dialog on wide screens (D6). Verify: at 1280 px "Post an idea" opens centered, ≤ 512 px wide, over a dimmed page, closes with Escape and with a backdrop click; at 375 px it still slides up from the bottom; `prefers-reduced-motion` shows no transition in either.

## 3. Walkthrough, docs and the live check (≈45 min)

- [x] 3.1 Repeat the simplicity walkthrough at 1280 px on the three screens: exactly one yellow action kind per screen (computed-colour scan, inert sheets excluded), no jargon, tap targets ≥ 44 px, keyboard order follows the visual order (Tab through the top bar, then the left column, then the right). Verify: numbers recorded in this file; any failure fixed before 3.2.

  **Observed (1280 × 800).** Jargon: 0 matches on `/`, `/trade`, `/signals/{id}`. Controls under 44 px: none (the chart toolbar chips are inside a `role=group` and were excluded, as the constitution's check is about actions). Yellow actions: `/trade` → `["Buy 0.00002 BTC ≈ $1.59"]`; `/` and `/signals/{id}` → `["Copy this trade"]`. **Two failures found and fixed before moving on:** (a) the wide feed showed two kinds of yellow ("See it on the chart" per card *and* "Copy this trade"), fixed by making the card action secondary from `lg` (design D4b, spec scenario "One primary action on a wide screen"); (b) the tab order ran coins → chart → ticket while the layout read ticket → chart, fixed by putting the chart on the left and the ticket beside it, which removed the `order` classes entirely. Tab order now: `BTC` (one stop for the roving radiogroup) → chart toolbar → Up/Down → size → Buy, matching the visual order at every width. At 375 px the card action is yellow again and the Buy button's bottom edge is 740 with the nav at 755.
- [x] 3.2 README: "Try the deployed demo" mentions laptop and phone; a "Desktop" line in the status table; project structure (`AppNav`, `DetailRail`, `FeedRail`, `useMediaQuery`); a 1280 px screenshot of the feed next to the phone one. Interview guide gains the section. Verify: `grep -n BottomNav README.md` → none.
- [x] 3.3 After the push and the Vercel deploy: at 1280 px on the live URL the feed shows two columns and the top bar; at 375 px the bottom bar is opaque with icons and the feed is unchanged. Record the deployment in the commit body.

## 4. Change review

- [x] 4.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with one extra question: does any component fetch or render the second column below 1024 px. Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded here.

  **P-R result (2026-09-07): no findings left.** The extra question: `DetailRail` returns `null` before rendering `SignalDetail` (whose poll only exists while mounted) and `FeedRail` passes `null` as the poll url until wide, so nothing is fetched or rendered below 1024 px — verified in the network log (0 requests to `/api/signals` on `/signals/{id}` at 375 px). No write path changed: the only POST in the diff is `TradeForm`'s existing order submit, now receiving `mid` as a prop instead of polling for it. No `any`, `@ts-ignore`, empty `catch`, `NEXT_PUBLIC_` or `MAINNET`. Typecheck, lint, 114 tests and the production build are green. Three defects were found and fixed inside the change: the wide Trade layout that pushed the button to 908 px, two kinds of yellow action on the wide feed, and a tab order that did not follow the visual order (all recorded in 2.2 and 3.1; `design.md.old`, `proposal.md.old` and `spec.md.old` keep the superseded versions).
