## 1. The arithmetic, before any of it is drawn (≈45 min)

- [ ] 1.1 Create `lib/learn/simulate.ts` with the three pure functions from design D1: `frames(depth, frameBytes, stackBytes)` returning `{ used, fits, share }`; `deliver(updates, lossPercent, protocol, seed)` returning `{ arrived, newestAgeMs, stalls }` using a small seeded generator rather than `Math.random`; and `score(answers)`. Add `lib/learn/simulate.test.ts` covering: a depth that fits and one that does not, the exact boundary, a zero or negative depth; `deliver` at 0 % loss (both protocols deliver everything, no stalls), at 100 % loss, TCP arriving complete but with a later `newestAgeMs` than UDP at the same loss, UDP arriving incomplete but fresher, and **the same seed producing the same run twice**; and `score` on all-correct, all-wrong and empty. Verify: `pnpm test` green, `pnpm typecheck` exits 0.
- [ ] 1.2 Create `lib/learn/content.ts`: the memory visualiser's step sequence as a constant array (design D2), tracing the real call path `placeMarketOrder → toValidOrderSize → floorToLot → toChainUnits`, plus the six quiz questions with their answers and one-line explanations. Read the actual functions in `lib/decibel/orders.ts` and `lib/decibel/units.ts` while writing the sequence so the frames named are frames this app really pushes. Verify: a test asserts the sequence starts and ends with an empty stack, that every frame pushed is popped, and that each quiz question has exactly one correct answer.

## 2. The three interactives (≈2 h)

- [ ] 2.1 `components/learn/MemoryVisualizer.tsx`: steps through `lib/learn/content.ts` with forward and back controls, drawing the stack as a column that grows downward and the heap as a separate box, each with what frees it (a collector in the browser, scope in Rust). Stepping back from the first step does nothing. Verify in the browser at 1440 px and 375 px: stepping to the end and back returns to the initial state; when the allocating frame pops, the heap box is still shown with its label.
- [ ] 2.2 `components/learn/DepthSlider.tsx`: a depth control reading `frames()`, showing the stack filling, the share used, and — past the limit — both languages' real error text (design D3, which never actually recurses). A Recursive/Iterative switch shows the same algorithm both ways and reports the iterative one as fitting at the control's maximum. A "copy loop" variant reframes it as an idea whose copy is itself copyable. Verify: at the maximum depth, recursive reports an overflow with both error strings and iterative reports that it fits.
- [ ] 2.3 `components/learn/PacketRace.tsx`: two lanes over one `setInterval` (design D4) walking the plan from `deliver()`, with a loss-rate control, per-lane counts of what arrived and how old the newest is, and the closing question about which lane suits a price and which an order. With `prefers-reduced-motion` it jumps to the final state without starting the timer. Verify: at 0 % both lanes deliver everything; raising the loss makes TCP complete-but-older and UDP fewer-but-fresher, with both figures on screen; the timer is cleared on unmount (navigate away mid-run and confirm no further updates in the console).

## 3. The page (≈1 h)

- [ ] 3.1 `components/learn/CodeCompare.tsx` (design D5): two labelled snippets, side by side from `lg:` and one at a time with a switch below, the second pane labelled "Rust — for comparison". No syntax-highlighting dependency. Verify at 375 px: one language at a time and the page does not scroll horizontally.
- [ ] 3.2 `components/learn/Quiz.tsx`: six questions from `content.ts`, each marked right or wrong on the tap with its explanation, a running score, and a retry that resets without reloading. Green and red only for correct and incorrect. Verify by keyboard alone: every option reachable, focus visible, retry clears every answer.
- [ ] 3.3 `app/learn/page.tsx`: a Server Component (design D6) laying out the three sections in order — one-sentence opening, interactive, TypeScript/Rust comparison, "in Copy-Trade Lite", one-line closing summary — then the quiz. English throughout. Each section's interactive sits above its prose. Verify in the browser at 1440 px and 375 px: the order is right, nothing overflows horizontally, and the navigation bar shows its two tabs with neither highlighted.

## 4. Keeping it unlinked and keeping the repo TypeScript (≈20 min)

- [ ] 4.1 Add a test asserting `AppNav`'s tab list is exactly `/` and `/trade` (design D7), so a later footer link cannot quietly add a third. Verify: `pnpm test` green; then `git grep 'href="/learn"'` returns nothing outside this change's own artifacts.
- [ ] 4.2 Verify the language rule holds after the change: `git ls-files | grep -E '\.rs$'` empty, no Rust tooling in `package.json`, the dependency list byte-identical to before this change, and `pnpm typecheck`, `pnpm lint`, `pnpm test` and `pnpm build` all green. Read each Rust snippet back against the language's documentation and record here that it was checked — it is never compiled, so nothing else will catch an error in it.

## 5. Documentation (≈20 min)

- [ ] 5.1 README: a short section saying `/learn` exists, that it is reachable only by URL, that it serves **no tier of the brief** and is separate material, and that the Rust on it is quoted for comparison rather than built. Project structure gains the new files. `GUIA_ENTREVISTA_TS.md` (outside the repo) gains its section. Verify: the README does not imply `/learn` is part of the product.

## 6. Change review

- [ ] 6.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with four extra questions for this change: does the page import anything from `lib/decibel/` or `lib/signals/`, does it fetch anything, is yellow still reserved for one action per screen, and does every interactive clean up after itself. Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded here with the evidence.
