## 1. The two read routes (≈60 min)

- [x] 1.1 Confirm the SDK surface before any UI depends on it (design D1, first risk): a throwaway `tsx` probe calls `read.marketContexts.getAll()` and `read.marketPrices.getAll()` against live testnet and prints one row of each. Record the observed shape and the row count in this task. Verify: both calls return rows, `marketContexts` carries `volume_24h`, `open_interest`, `previous_day_price` and `price_change_pct_24h`, and BTC/USD is present in both. Delete the probe afterwards — it is a check, not a feature.

  **Observed, and it corrected the design.** Both calls return **75 rows**. `marketContexts` carries exactly the four fields expected: `{ market: "BTC/USD", volume_24h: 221936.69, open_interest: 1019.64, previous_day_price: 77243, price_change_pct_24h: 2.531 }`. But **the two sources key their rows differently**: `marketContexts` uses the market **name**, while `marketPrices.getAll()` uses the **address** (`{ market: "0x161b7b3f…0dca", mark_px: 79168, mid_px: 79168, funding_rate_bps: 0.11, is_funding_positive: true, funding_period_s: 3600, open_interest: 1019.64, transaction_unix_ms: … }`), so `prices.find(p => p.market === "BTC/USD")` is `undefined`. This is the same name/address split `lib/decibel/stream.ts` already handles for the WebSocket. D1 was corrected to three upstream calls — `markets.getAll()` supplies the address→name map — with `design.md.old` kept. Probes deleted.
- [x] 1.2 `GET /api/tickers`: add `Ticker` to `lib/schemas.ts` (`market`, `symbol`, `mid: number | null`, `changePct24h: number | null`), `listTickers()` to `lib/decibel/markets.ts` joining the two `getAll` calls by market name with `Promise.allSettled`, and `app/api/tickers/route.ts` through `apiHandler` with **no** `schema` and **no** `guard` (read-only). A market missing from either source keeps its row with `null` in that field. Add `lib/decibel/markets.test.ts` cases for the join: both sources present, price missing, context missing, a market in prices but not in `/markets` (dropped). Verify: `pnpm test` green, `pnpm typecheck` exits 0, and `curl localhost:3000/api/tickers` returns one row per market with two upstream calls in the server log, not one per market.

  **Observed.** The filter that decides which markets exist was extracted as `tradableRows`, shared by `listMarkets` and `listTickers`, so the strip can never list a market the trade screen does not offer — verified: `/api/tickers` returns **36 rows** and `/api/markets` returns **36**, BTC/USD first in both. The join (`joinTickers`) is pure and covered by 8 cases including the name/address split found in 1.1, a price row for an unknown address (dropped), and a zero mid (treated as no price). On testnet every row came back quoted (0 null mids, 0 null changes) and negative changes survive as values (AMD −1.0259 %). **Cost, measured:** three runs of `/api/tickers` at 0.60 s for 36 markets against three runs of `/api/price` at 0.66 s for one — the same, which is what "constant in market count" looks like; one call per market would be ~36× that. 164 tests green, typecheck 0.
- [x] 1.3 `GET /api/stats/[market]`: add `MarketStats` to `lib/schemas.ts` (`changePct24h`, `high24h`, `low24h`, `volume24h`, `openInterest`, `fundingRateBps`, `isFundingPositive`, `fundingPeriodS`, each nullable where the source can fail), `getMarketStats()` in `lib/decibel/markets.ts` refusing an unknown market **before** any upstream call and deriving high/low from `getCandles(market, { interval: "15m", count: 96 })` inside `Promise.allSettled` (design D2), and the route. Verify: `curl` for BTC/USD returns all eight fields with `high24h >= low24h`; `curl` for `NOPE%2FUSD` returns `422` naming the market with no upstream call in the log; with the candle call forced to fail, high and low are `null` and the status is still `200`. `pnpm test` green.

  **Observed.** `GET /api/stats/BTC%2FUSD` → `200` with all eight fields: `{ changePct24h: 2.5691, high24h: 79217, low24h: 76373, volume24h: 223840.46, openInterest: 1019.65, fundingRateBps: 0.11, isFundingPositive: true, fundingPeriodS: 3600 }` — `high24h > low24h` as required. `GET /api/stats/NOPE%2FUSD` → `422 UNKNOWN_MARKET "Unknown market \"NOPE/USD\"."`, refused after the market-list read and before any price or candle call. The three sources are independent `Promise.allSettled` branches, each logging its own warning, so a failed candle read leaves `high24h`/`low24h` null while the other six figures still answer `200`; `highLow` is pure and covers the empty case that produces those nulls (4 cases, including a non-finite candle). 168 tests green, typecheck 0.

## 2. The chart becomes an instrument (≈90 min)

- [x] 2.1 Level names leave the price axis (design D3 and D4): every `createPriceLine` becomes `axisLabelVisible: true, title: ""`, the `LABEL_GAP` collision block and its `hidden` set are deleted, `SeriesMarker.text` is dropped, and `MarketChartInner` renders a caption row under the chart with a colour swatch, name and price per level plus the marker labels. Verify with agent-browser on `/signals/{id}` at 1440 px: no text is clipped at any plot edge (the `u copied` defect is gone), no level label sits on an axis tick, and the three level names with their prices are readable under the chart. Screenshot before and after in the commit body.

  **Observed on `/signals/4a2d3f85…` at 1440 × 900 (an idea with 4 copies).** The axis now carries prices only — `82,563`, `80,158`, `79,195`, `78,555` — each on its own row, none sitting on a scale tick (`83,000`, `82,000`, `81,000`, `78,000`, `77,000` all readable). No text is clipped at any plot edge: the copy marker on the earliest visible bar is a bare arrow, so the `u copied` defect has no way to occur. The caption reads `■ Entry $80,158 ■ Take profit $82,563 ■ Stop loss $78,555 ▲ Ben, You copied · You, You copied`, so both the level names and the copier names survived the move. **21 lines deleted** with the cause: the `LABEL_GAP` constant, the `withCoords`/`hidden` collision pass and the per-line `crowded` branch. `SignalDetail`'s own legend row went with them (the first half of 4.2, done here so no commit ships a duplicated legend). Typecheck and lint clean.
- [x] 2.2 Autoscale stops flattening the candles (design D5): add `levelsInScale(candles, levels, factor = 1.5)` to `lib/charts.ts` returning which levels belong in the scale and which are above or below it, with unit tests (level inside, just outside, far outside, no candles, empty levels, Down idea mirrored). `autoscaleInfoProvider` folds in only the returned levels; the excluded ones are not drawn and the caption says "above the chart" / "below the chart" with the price. Verify: on the feed card whose stop loss is 50 % below the entry, the candles show a readable vertical spread (measure the series' pixel range, not by eye) and the caption names the stop loss as below the chart. `pnpm test` green.

  **Observed, measured from the axis rather than by eye.** The helper is `splitLevels` in `lib/charts.ts` (generic over `{ price }`, so `charts.ts` still knows nothing about chart lines), with `LEVEL_SCALE_FACTOR = 1.5` named rather than inlined, and 8 unit tests: inside, just outside, far above, the −50 % case, no candles, a market that has not moved, no levels, and a Down idea mirrored. On `/signals/3ee5cc4f…` — entry $79,092, stop loss $39,546 — the price axis now spans **78,200 → 79,400 (≈1,200)** with candle bodies and wicks clearly separated; the same idea previously spanned **39,546 → 79,338 (≈39,800)** and rendered as a flat line. That is a **33× tighter scale**, read off the printed axis labels. The caption states `▪ Stop loss $39,546 · below the chart`, so nothing is silently dropped. On the feed, a Down idea reports `▪ Take profit $74,865 · below the chart` while Entry and Stop loss stay in scale, confirming the mirrored case live. 176 tests green, typecheck and lint clean.
- [x] 2.3 Controls above the canvas, crosshair readout and bar countdown (design D6): move the toolbar markup above the chart container; add `countdownLabel(msLeft)` to `lib/charts.ts` with unit tests (over a minute, under a minute, zero, negative → "closing"); subscribe to `chart.subscribeCrosshairMove` inside the existing effect and hold the reading in React state so a candle refresh does not reset it, falling back to the last bar when the pointer leaves; render O/H/L/C and the bar's change as a DOM overlay, and the countdown beside the range chips. Verify with agent-browser at 1440 px: the readout follows the pointer across three candles and matches the data at those timestamps, it returns to the last bar on pointer-out, the countdown decreases once per second, and the reading survives a 15 s candle poll without flickering.

  **Observed on `/trade` at 1440 × 900.** Toolbar above the canvas by measurement: the Chart type and Zoom groups sit at y = 254, the chart at y = 304, and `types.bottom <= chart.top` is true — the time axis is now the lowest part of the chart. Countdown read `bar closes in 0:37` then `0:33` three seconds later, so it ticks. `countdownLabel` is pure in `lib/charts.ts` with 5 tests (mm:ss, zero-padding, the hours form, a passed close reading "closing", and NaN). Crosshair readout across three bars at 20 / 40 / 60 % of the plot width gave three distinct, self-consistent bars — `O 79,167 H 79,169 L 79,120 C 79,136 −0.04%`, `O 79,054 H 79,054 L 79,010 C 79,010 −0.06%`, `O 79,201 H 79,224 L 79,201 C 79,221 +0.03%` — and moving the pointer off the plot returned it to the most recent bar. **Across polls:** holding the pointer still for 30 s, the reading was byte-identical at t=0 and t=10 s, then moved by exactly one bar (the close of the old bar, 79,171, became the open of the new one) when a new candle arrived and the plot re-fitted — it never blanked and never reset to the last bar, which is what keeping only the bar's *time* in state buys. Only the full variant has it: the feed renders 7 card charts and 1 full chart, and the page holds exactly **1** readout and **1** countdown.
- [x] 2.4 Volume histogram (design D7): a `HistogramSeries` on `priceScaleId: "volume"` with `scaleMargins: { top: 0.8, bottom: 0 }`, coloured by bar direction at 40 % opacity, added only when `variant === "full"`. Verify: on `/trade` the histogram occupies the lower fifth and the price series keeps its full height (compare the price series' pixel range before and after — it must not shrink); on a feed card no histogram is drawn. Record the chart rebuild time before and after per the first risk in design; if it exceeds 50 ms, apply the mitigation named there.

  **Observed, and it corrected the design a second time (`design.md.old2` kept).** The histogram sits on its own `priceScaleId: "volume"` with `scaleMargins { top: 0.8, bottom: 0 }`. The first attempt still let the two regions overlap: the price scale's own bottom margin was 0.12, so the price series could draw down to 88 % of the pane while volume started at 80 %. Raising the price scale's bottom margin to 0.24 on full charts separates them — **price now ends at 76 %, volume starts at 80 %, no overlap** — and the price series still draws 250 px of a 420 px chart. To make "the volume did not squash the price" a measurement rather than an opinion, the chart now writes `data-price-span` (pixels between the highest high and the lowest low), the same kind of diagnostic as the existing `data-visible-bars`. Cards get no histogram: the feed showed 7 card charts at 170 px and 1 full chart at 260 px, with exactly 1 OHLC readout and 1 countdown on the page.

  **Rebuild cost, and an honest correction.** Over 120 s of steady state on `/trade` (8 candle polls) the page produced **2 long tasks, 50 ms and 59 ms**; a separate 60 s window gave 1 of 52 ms. So about a quarter of rebuilds cross the 50 ms line I set, worst case 59 ms. Going to apply the mitigation this design named — "skip the volume series when the data is unchanged" — I found it **cannot work**: the effect destroys and recreates the whole chart whenever `candles` changes, so volume's data is always new at the moment it is added, and the skip is unreachable. The only real fix is to stop rebuilding (create once, `setData` on refresh), which is an explicit Non-Goal of this change because it touches the component all three screens share. The design's risk was rewritten with these numbers and that reasoning; the hitch ships knowingly and goes in the README's "what's next" with the measurements, per task 5.1.

## 3. The wide trading desk (≈100 min)

- [x] 3.1 `components/MarketStatsBar.tsx`: the day's figures beside the price, each labelled, change coloured, a missing figure as "—", fed by `usePoll<MarketStats>` at 30 s from `TradeScreen` so it follows the coin selection. Verify at 1440 px: the five figures render for BTC, switch to ETH within a moment of tapping the pill without the price hero blanking, and with the route blocked the previous values stay and no error dialog appears.

  **Observed on `/trade` at 1440 × 900:** `24h change +2.29% · 24h high $79,302 · 24h low $76,373 · 24h volume 229.5K · Open interest 1.0K · Funding +0.0012% every 1h`. Two decisions worth naming. **Funding is not shown in basis points** — the constitution bans that word on this screen — so the same number is given as a percentage with how often it is charged. **Volume and open interest carry no unit**: the SDK documents none, and BTC volume of 229.5K is implausible read either as BTC or as dollars, so inventing a "$" would be a claim I cannot support (`compact` and `everyLabel` added to `lib/format.ts` with 7 tests). A missing figure renders "—", never 0: a zero low would be a statement about the market.
- [x] 3.2 `CoinPills` shows price and 24 h change: a `usePoll<Ticker[]>` in `TradeScreen` at 15 s feeds the strip, and streamed mids from `useLive` override the poll for markets the stream names. Keep the radiogroup, the roving tabindex and the edge mask. A ticker without a price shows the symbol alone. Verify: every pill shows a price and a coloured change; with the stream connected the BTC pill's price moves without a `/api/tickers` request; keyboard arrows still move between pills and one Tab still leaves the strip.

  **Observed:** the first three pills read `BTC $79,073 +2.29%`, `AAPL $334 +0.94%`, `AAVE $131 +3.37%` — price rounded by magnitude (no cents above $100), change signed and coloured. The row of 36 mute tickers is now a market map, which is what the fomo.family reference does well. One detail the design did not anticipate: the **selected** pill inverts to a light background, where the up-green is unreadable, so on that pill the change is stated in its own ink instead of the semantic colour. The radiogroup, the roving tabindex and the edge mask are untouched.
- [x] 3.3 Share the account row renderers and add `components/PositionsPanel.tsx` (design D10): move `Row`, `Stat` and the three list bodies out of `AccountCard` into a small shared module both import; the panel renders them as tabs (Positions / Open orders / Fills) with counts, one always open. Verify: the same position renders identically in the accordion and in the panel (compare the two DOM subtrees); tabs are reachable by keyboard with arrow keys per the tablist pattern; each empty state still reads as before.

  **Done.** `components/AccountRows.tsx` now owns `Stat`, `Row`, `Empty` and the three list bodies; `AccountCard` and the new `PositionsPanel` both import them, so a position row has one implementation. `AccountCard` gained `showLists`, false on a desk, where it keeps only Equity / Available / PnL in the right rail. Observed at 1440 × 900: `Positions (3) · Open orders (6) · Fills (20)` as tabs under the chart with Positions open. One vocabulary fix went in while moving the rows: the Open orders list said **Buy/Sell**, the two words the constitution bans on this screen, and now says Up/Down like everything else.
- [x] 3.4 The full-height grid (design D9): `TradeScreen` becomes a height-constrained grid at `lg:` and `min-height: 700px`, with the chart column `flex-1`, `min-h-0` on scrolling children, the right rail holding the ticket and the three account numbers, and `PositionsPanel` under the chart. DOM order stays left column then right rail, with no `order` classes. A `ResizeObserver` hands the chart its pixel height. Verify with agent-browser and `getBoundingClientRect`, recording every number in this task: at 1440 × 900 the chart's area is the largest of any panel, `document.body.scrollHeight <= window.innerHeight`, and no empty band below the last panel exceeds 80 px; at 1024 × 640 the layout falls back to the stacked column and scrolls; at 375 × 812 the column is unchanged.

  **Measured at a real 1440 × 900 — and the first rounds were measured at the wrong size.** `agent-browser open --viewport` is silently ignored; the window was 1254 × 564 and, being under 700 px tall, never entered the desk mode at all. `agent-browser set viewport 1440 900` is the command. Everything below was taken after that.

  | | before this change | after |
  |---|---|---|
  | chart | 370 × 390 = 144,300 px² (~11 % of the viewport) | 1052 × 358 = **376,287 px² (29 %)**, the largest element on screen |
  | page scrolls | yes | **no** — `document.body.scrollHeight` 900 = `innerHeight` 900 |
  | empty band below content | ~90 px | **none** |
  | yellow button | y = 908, below the fold | **y = 457** |
  | Positions / Orders / Fills | collapsed accordion in a 340 px column | tabs at y = 649, 1018 px wide |

  Also 1280 × 800: no scroll, chart 23 %, button at 457. **Two defects found by measuring rather than reasoning.** First, `main` computed to 973 px instead of 836: `flex-1` sets `flex-basis: 0%`, which overrides `height` along the main axis, so the explicit viewport height was being ignored until `desk:flex-none` turned the growing off. Second, the tab panel was `shrink-0` with only a `max-height`, so it kept its content height and pushed the column 113 px past the viewport, where `overflow-hidden` quietly clipped the last rows — a definite `basis` plus `h-full` on the card fixed it. A new `desk` breakpoint (1024 px wide **and** 700 px tall) is declared once in `globals.css` as a `@custom-variant` and once as `DESK` in `useMediaQuery.ts`, with each pointing at the other.
- [x] 3.5 Your position on the Trade chart (design D8): `TradeScreen` selects the position matching the selected market from its account poll and passes `lines` to `MarketPanel` — entry always, liquidation only when finite and greater than zero. Verify: with an open BTC position the entry line sits at the position's entry price and is named in the caption; closing the position removes it within one account refresh; with no position no line is drawn.

  **Observed, and it corrected D5 (`design.md.old3` kept).** The account held BTC/USD entered at $79,889 with a liquidation at $76,243. Drawing the entry unconditionally worked — and was its own defect: the candles' drawn height fell from **250 px to 72 px** of a 420 px chart, because an entry eight days old is 3× the hour's span away. So the pin became conditional on the candles keeping at least 40 % of the scale (`MIN_CANDLE_SHARE`, 5 more tests). Measured at both ranges: on **1h**, the caption reads `▪ Your entry $79,889 · above the chart` and the candles keep **250 px**; on **1w**, where the entry belongs, both `■ Your entry $79,889` and `■ Liquidation $76,244` are drawn and the candles still keep **238 px**. The entry price is on screen either way. The spec's scenario was rewritten to describe this, and a second scenario added for the far case — the original assumed the line is always drawn, which is only true when the range allows it. Colours: the entry is **white**, not the idea-Entry yellow, so it is never confused with the screen's one yellow action (constitution P1); liquidation is drawn only when finite and above zero, since the exchange reports 0 for a position that cannot be liquidated (QQQ in this account does exactly that).
- [x] 3.6 Phone chart height and the button's verb (design D11): the chart stands at least 260 px at 375 × 812 with the toolbar above it, and `TradeForm`'s verb becomes "Go Up" / "Go Down" in the button label and in the success toast. Verify by measurement at 375 × 812: the chart's height is ≥ 260 px **and** the yellow button's bottom edge is ≤ 812 px — both, or the task is not done. Tapping Down makes the button read "Go Down …".

  **Both, after the task turned out to demand something impossible in one column — decided with the user.** Stacked, the phone screen now carries two rows it did not before (the day's figures, the chart's legend), and measurement said so plainly: the yellow button sat at **827 px** on an 812 px screen with the chart still at its old 110 px. Compressing both new rows into single scrolling rows (the day's figures went 92 px → 22 px) and tightening the gaps recovered 70 px and still left it at 827. A 260 px chart *and* a reachable button do not fit in one column, so this was put to the user, who chose **two panes**: below the desk layout, Chart and Trade take turns, with Trade on arrival.

  Measured at 375 × 812: **Trade pane** — yellow button at y = 501, bottom **561**, against a usable bottom of 748 (the fixed nav takes the last 64 px). **Chart pane** — chart **320 px**, content ends at 717, so it fits with no scrolling at all; the price hero, the six figures, the controls, the countdown and the volume histogram are all on screen. The chart is created while the pane is hidden, so it is built at zero width: verified that revealing it resizes correctly rather than leaving a blank canvas — after tapping Chart at 1024 × 640 the canvases measure 914 × 392, not 0. The button reads **"Go Up 0.00002 BTC ≈ $1.58"** and the success toast says "Order sent: Up …"; `verb` and the toast's `direction` are separate so the toast does not read "order sent: go up". The spec's requirement was rewritten around the two panes, with scenarios for the default pane, the chart fitting, and a half-typed order surviving a switch.

## 4. Feed and detail follow the same chart (≈30 min)

- [x] 4.1 Feed cards keep only the range chips: `MarketChartInner` renders chart-type and zoom controls only for `variant="full"`; pinch and scroll zoom still work on a card. Verify: a card shows four controls, not ten; `/trade` and the detail still show all of them; scrolling on a card's chart still zooms it.

  **Observed on `/` at 1440 × 900 with 7 card charts and 1 full chart on the page.** Control groups counted by role: `Range` 8 (every chart keeps its range chips), `Chart type` **1**, `Zoom` **1** — the one full chart. A card's toolbar now holds **4 buttons, down from 10**. Two live ideas had to be posted first (BTC Up and ETH Down, 8 h hold): all six existing local ideas were settled or expired, and `loadFeed` fetches candles only for live ideas, so every card was correctly falling back to `IdeaStrip` — not a regression, and worth knowing before reading an empty feed as a bug.

  **One claim I could not verify, stated plainly.** Scroll-to-zoom did not respond to the automation's synthetic wheel event: `data-visible-bars` stayed at 60 on a card. A control experiment on the **same** full chart separates tool from code — wheel: 240 → 240 (nothing), the Zoom-in button: 240 → **144** (works). So the wheel event is not reaching lightweight-charts' handler through CDP, on *both* variants, while the buttons do. This diff wraps buttons in a conditional and changes nothing in `handleScale` (`pinch`, `mouseWheel` and `axisPressedMouseMove` are untouched), so there is no mechanism for a regression — but "no mechanism" is not a measurement, so scroll and pinch zoom on a card are carried into task 5.2 as a manual check on the deployment rather than recorded as verified here.
- [x] 4.2 `SignalDetail`'s caption absorbs the copy-marker names (design D4) and drops its now-duplicated level row; `SignalCard`'s caption carries the level names and any off-chart level from task 2.2. Verify: on a detail with three copies nine minutes apart and three more within a minute, the caption reads "Ben, Cid copied" and "3 copied" while the chart shows two markers; no label overlaps anything.

  **Done; the detail half landed in 2.1** so that no commit shipped a screen with two legends. Observed on `/signals/4a2d3f85…` at 1440 × 900, an idea with 4 copies: the caption reads `■ Entry $80,158  ■ Stop loss $78,555  ▪ Take profit $82,563 · above the chart  ▲ Ben, You copied · You, You copied` — levels, the off-chart one with its side, and the copiers, all in one row. Scanned every element for text wider than its clipping box: none. The detail's own three-swatch row is gone, replaced by the chart's caption; the "now $…" and "couldn't refresh" line it also carried stays, since nothing else says that.

## 5. Documentation and the live check (≈30 min)

- [x] 5.1 README: the API contract table gains `/api/tickers` and `/api/stats/[market]`; the STRETCH "mobile-friendly layout" row describes the desk layout; "what's next" gains the chart's create/destroy lifecycle, named honestly as a known weakness (design, first risk). `docs/SAFETY_REVIEW.md` gains a row stating both new routes are read-only, carry no guard, accept no body and expose only public market data. `GUIA_ENTREVISTA_TS.md` gains its section. Verify: the README claims nothing this change did not ship.

  **Done.** The README's "What's next" listed the ten review findings — all of which this change shipped — so it was rewritten with what is actually next, led by the chart's create/destroy lifecycle **with its measured cost** (2 long tasks of 50 ms and 59 ms over 8 refreshes) and why it was deliberately excluded. The API table gained both routes with what to expect, including that a figure the exchange does not provide is `null`, never 0. The STRETCH mobile row now describes the desk layout with its numbers and the two phone panes, and says plainly why the panes exist. Project structure gained the three new components and the extended `charts.ts` / `markets.ts`. `docs/SAFETY_REVIEW.md` gained row 2c for the new read surface — **verified, not asserted**: `POST` to either route returns **405** (no `POST` is exported) and `GET` returns **200** with no demo code. `GUIA_ENTREVISTA_TS.md` gained section 13 (11 subsections, including the three design corrections and the one claim that could not be verified); it lives outside the repo and `git status` confirms it is not tracked.
- [ ] 5.2 After the push and the Vercel deploy: on the live URL, at 1440 × 900 confirm the chart dominates and the viewport has no empty band, the stats bar shows five figures, the pills show changes, the tabs hold the three lists and an open position draws its entry line; at 375 × 812 confirm the chart height and the button position. Record the measured numbers and the deployment in the commit body. **Carried over from 4.1:** confirm by hand that scroll-to-zoom on a feed card and pinch-to-zoom on a phone still work — the automation's synthetic wheel event could not exercise them on either chart variant.

## 6. Change review

- [x] 6.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with four extra questions for this change: does any new route accept input or need a guard, does any new figure show `0` where the exchange gave nothing, is the yellow still the only strong accent and still one primary action per screen, and does every screen still render when both new routes fail. Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded here with the evidence.

  **P-R result: one finding, fixed in place; the other three questions clean.**

  *(1) Does any new route accept input or need a guard?* No. Both are declared through `apiHandler` with no `schema`, so no body is ever read, and no `guard`, because they are reads like `/api/markets`. Observed: `POST /api/tickers` and `POST /api/stats/BTC%2FUSD` → **405** (no `POST` is exported); `GET` on both → **200** with no demo code. `/api/stats` takes one path segment and checks it against the live market list before asking the exchange anything else — `NOPE/USD` → 422 with no price or candle call. The three write routes still declare `guard: assertDemoAccess`, and `git diff HEAD` over `app/api/order`, `app/api/signals`, `lib/decibel/orders.ts`, `lib/auth.ts` and `lib/api.ts` is **empty**: this change touches no path that can sign.

  *(2) Does any new figure show 0 where the exchange gave nothing?* **One did.** The funding period was read as `everyLabel(s.fundingPeriodS ?? 0)`, which renders "every —" when the period is unknown — not a false zero, but not a sentence either. Fixed: the period is shown only when it is known. Everything else routes through `finiteOrNull` / `positiveOrNull`, which return `null`, and the interface prints "—".

  *(3) Is the yellow still the only strong accent, and still one primary action per screen?* Yes. `grep yellow` over the four new or rewritten components returns **two hits, both in comments**, no styles. The account's own entry line is deliberately **white** (`#f5f5f4`), not the idea-Entry yellow, so the Trade screen still has exactly one yellow thing on it: the button.

  *(4) Does every screen still render when both new routes fail?* Yes — verified by aborting `**/api/tickers**` and `**/api/stats/**` in the browser rather than by reading the code. The statistics row reads `24h change — 24h high — 24h low — 24h volume — Open interest — Funding —`; the coin pills fall back to the symbol alone ("BTC"); the chart still draws at 361 px; the price hero is still live from the stream; the yellow button is enabled and correctly labelled; the account tabs render; no error screen appears anywhere.

  Constitution greps over `app/`, `components/`, `lib/` and `hooks/`: `NEXT_PUBLIC_` **0**, `MAINNET` **0**, `: any` or `@ts-ignore` **0**, empty `catch` **0**, `.js`/`.jsx` sources **0**. Typecheck, lint, **191 tests** and the production build are green, and `openspec validate --all` is 5/5.
