## 1. The rule, pure and tested (≈45 min)

- [x] 1.1 Create `lib/signals/outcome.ts` with `settleSignal(signal, candles, now)` per design D1 (Up and Down mirrored, both-levels-in-one-candle → `sl`, expiry → `expired`, still open → `null`, candles outside `[createdAt, min(now, expiresAt)]` ignored) and `outcomeLabel(signal)` returning the badge text and tone. Add `lib/signals/outcome.test.ts`: Up hits TP, Up hits SL, Down hits TP (low crosses), Down hits SL (high crosses), one candle crossing both → `sl`, hold ended with neither → `expired`, still inside the hold → `null`, empty candle list → `null` (or `expired` past the deadline), candles before `createdAt` ignored. Verify: `pnpm test` green with those cases.

## 2. Reading the history and storing the result (≈60 min)

- [x] 2.1 Add `getCandlesSince(marketName, sinceMs)` to `lib/decibel/markets.ts` (interval chosen by span per design D3, reusing the ladder in `lib/charts.ts`; export the span→interval helper from there with its own test). Verify: `pnpm typecheck` exits 0; with the dev server, a temporary script or route proves a 3-hour span returns 1-minute candles and a 3-day span returns 15-minute ones.
- [x] 2.2 Add `settleAll(signals, now, deps)` to `lib/signals/outcome.ts` (filter unsettled → group by market → earliest `createdAt` per market → one `getCandlesSince` per market with `Promise.allSettled` → `settleSignal` each → return the changed ones), with `deps` injectable so its test uses fake candles and never touches the network. Verify: `pnpm test` covers "two ideas of the same market cause one fetch", "a market whose fetch rejects leaves its ideas unsettled and does not throw", and "already-settled ideas are not fetched for".
- [x] 2.3 Add `setOutcome(id, outcome)` to `lib/signals/repo.ts` (`UPDATE signals SET outcome = ? WHERE id = ? AND outcome IS NULL`) and extend `authorStats()` with the `settled` and `won` columns per design D5; extend `AuthorStats` in `lib/schemas.ts`. Update `lib/signals/repo.test.ts`: setting an outcome persists it, setting it twice keeps the first, and `authorStats` counts settled/won correctly with copies present (the `LEFT JOIN` must not inflate them). Verify: `pnpm test` green.
- [x] 2.4 Run the pass inside `loadFeed()` (`lib/signals/feed.ts`): settle, write the changes, and return the signals already carrying their outcome. Verify with the dev server: post an idea with a take profit 0.01 % above the entry, reload the feed within a minute and `curl localhost:3000/api/signals` shows `outcome: "tp"` for it and `settled: 1, won: 1` for its author; a second reload makes no further candle request for it (server log).

## 3. Showing it (≈45 min)

- [x] 3.1 Badge in `components/SignalCard.tsx` and `components/SignalDetail.tsx` from `outcomeLabel` (up colour for `tp`, down for `sl`, muted for `expired`, today's "live · Xh left" while open), and the card's action switches to the secondary "See how it went" when settled, not when merely past its deadline. Author summary shows "N of M hit" when `settled > 0` and nothing when it is 0. Verify with agent-browser at 375 px: a settled idea shows its badge and the secondary action; a live one is unchanged; no new saturated colour on screen.
- [x] 3.2 A settled idea cannot be copied: `CopyPanel` disables with the outcome text, and `POST /api/signals/[id]/copy` refuses any settled idea (extend the existing expiry check). Verify: `curl` a copy of a settled idea → 422 with a readable message and no order placed; the button is disabled in the browser.

## 4. Documentation and the live check (≈30 min)

- [x] 4.1 README: status row for the STRETCH "outcome marking" moves to done with what it does; the demo path mentions the badge; a line in Safety about settling from candles and the conservative both-levels rule; `docs/SAFETY_REVIEW.md` rule 6 ("don't fake it") gains the same note. Interview guide gains its section. Verify: the README states the both-levels rule in one sentence.
- [ ] 4.2 After the push and the Vercel deploy: on the live URL, an idea that has hit its level shows the badge, its author shows the record, and a settled idea's copy button is disabled. Record the deployment in the commit body.

## 5. Change review

- [x] 5.1 Run the P-R review prompt on the diff against `specs/constitution.md`, with two extra questions for this change: can settlement ever mark an idea `tp` from a candle that also crossed the stop, and can a settled idea reach `placeMarketOrder` through any path. Verify: findings fixed in a separate `fix:` commit, or "no findings" recorded here.

  **P-R result (2026-09-07): no findings left.** Extra question 1 — *can a candle that crossed both levels be recorded as `tp`?* No: `hitSl` is evaluated and returned before `hitTp` in the same iteration (`outcome.ts:45–46`), the loop returns on the first deciding candle, and a test asserts `sl` for both an Up and a Down idea with a both-crossing candle. Extra question 2 — *can a settled idea reach `placeMarketOrder`?* No: the only two paths to it are `POST /api/order` (which never reads a signal) and `POST /api/signals/[id]/copy`, whose guard is now `isExpired(signal) || signal.outcome !== null`, verified live with curl (422, no order). The UI disables the button and both inputs as well, but the route is the barrier. Settlement itself performs no writes to the exchange: `lib/signals/outcome.ts`, `lib/charts.ts` and the two touched components contain no `placeMarketOrder`, `postEnvelope` or `POST`. No `any`, `@ts-ignore`, empty `catch`, `NEXT_PUBLIC_` or `MAINNET` anywhere in `app/`, `components/`, `hooks/`, `lib/`, `scripts/`. Typecheck, lint, 137 tests and the production build are green. One defect was found and fixed during the change: filtering the feed's candles by `outcome === null` made a freshly settled idea lose its chart mid-life; the filter is back to the deadline only.
