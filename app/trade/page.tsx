// Placeholder; the real Trade screen lands with the trade-screen change.
export default function TradePage() {
  return (
    <main className="mx-auto flex w-full max-w-lg flex-1 flex-col gap-3 p-6">
      <h1 className="text-4xl">Trade</h1>
      <p className="text-muted">
        The trade screen is coming next: pick a coin, Up or Down, how much, one button.
      </p>
    </main>
  );
}
